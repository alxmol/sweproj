# Pinned by Mini-EDR ml-pipeline-worker. Updated via this file only.
# Keep this ONNX stack pinned together: onnxmltools 1.13.0 + onnx 1.17.0
# triggered a boolean-attribute serialization failure during real XGBoost tree
# conversion, while onnxmltools 1.16.0 + onnxconverter-common 1.16.0 converts
# the fitted XGBClassifier into a real TreeEnsemble ONNX graph successfully.
xgboost==2.1.3
onnx==1.17.0
onnxmltools==1.16.0
onnxconverter-common==1.16.0
onnxruntime==1.20.1
scikit-learn==1.5.2
pandas==2.2.3
numpy==2.1.3
ruff==0.7.4
